<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Template</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f6f6f6;
            margin: 0;
            padding: 0;
        }

        .email-container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .email-header {
            font-size: 20px;
            color: #333333;
            font-weight: bold;
        }

        .email-subheader {
            font-size: 14px;
            color: #888888;
            margin-bottom: 20px;
        }

        .job-item {
            margin-bottom: 15px;
        }

        .job-item h3 {
            font-size: 16px;
            color: #007bff;
            margin: 0;
            text-decoration: none;
        }

        .job-item h3:hover {
            text-decoration: underline;
        }

        .job-item p {
            font-size: 14px;
            color: #555555;
            margin: 5px 0;
        }

        .cta-button {
            display: inline-block;
            background-color: #d32f2f;
            color: #ffffff;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
            margin-top: 20px;
            text-align: center;
            font-size: 16px;
        }

        .cta-button:hover {
            background-color: #c62828;
        }
    </style>
</head>

<body>

    <div class="email-container">
        <div class="email-header">Hỏi Dân IT</div>
        <div class="email-subheader">Ít nhưng mà chất</div>

        <p>Hi {{receiver}},</p>
        <p>One of these jobs may be your next one. Find out today!</p>

        {{#each jobs}}
        <div class="job-item">
            <h3><a href="#">{{name}}</a></h3>
            <p>{{company}}</p>
            <p>{{salary}}</p>
            {{#each skills}}
            <p>{{this}}</p>
            {{/each}}
        </div>
        {{/each}}



        <a href="#" class="cta-button">Xem thêm job tại Hỏi Dân IT</a>
    </div>

</body>

</html>